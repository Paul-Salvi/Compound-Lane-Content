# public/ — Static assets for the DCA explainer composition

| File | Role | Source |
|------|------|--------|
| `page-background.png` | The frozen notebook page (1080×1920). Fills the composition for the full 91s. | Copied from `02-visual/dollar-cost-averaging.png` |
| `hand-pointer.svg` | The animated hand pointer. The fingertip is the tap target; the rest of the hand trails. | Hand-authored SVG in the notebook's hand-drawn style. Drop-shadowed, with skin tone and palm detail. |

All assets here are referenced from `compositions/dca-explainer/index.html` via `data-*` timing attributes and the `class="clip"` contract.
