# Taste — Coding, Tooling & Workflow Preferences
- Prefers to provide source content as a local file rather than having the assistant web-fetch from URLs; explicitly instructs "dont webfetch" when content is already saved locally. Confidence: 0.9
- Wants comprehensive documentation that captures both required actions AND explicit don'ts/pitfalls, so repeatable workflows can be executed at "lightning speed" on future tasks. Confidence: 0.85
- Values investing in upfront documentation/guides to optimize speed and consistency across recurring/repeated creative tasks. Confidence: 0.8
- Prefers a **single source of truth** for production knowledge (e.g. a top-level `GUIDE.md`) that agent instruction files (`AGENTS.md`/`CLAUDE.md`) **cross-reference** rather than duplicate inline; keeps agent context slim but always pointing at the canonical, up-to-date workflow. Confidence: 0.75
- Expects agent context files to be **kept in sync** with newly captured production guides — when a guide is created/updated, `AGENTS.md`/`CLAUDE.md` should be edited to carry the same learnings (commands, pitfalls, audio setup), and new projects should get their own copies — explicitly requested with "also update the guide and agent" after a task, so future sub-agent runs inherit the latest knowledge; the user then verifies the **root-level** docs were updated ("Does the agent and guid updated at root?"), treating root `AGENTS.md`/`GUIDE.md` as the canonical knowledge store. Confidence: 0.9
- Wants to provide all required project inputs **upfront via a structured fill-in-the-blank checklist** (Source, Type, Length, Format, Voice/Speed, Brand, Fonts, Logo, Hook, Key data, CTA, Don'ts) rather than through conversational Q&A; this minimizes back-and-forth so the assistant can work at "lightning speed." Confidence: 0.85
- Prefers portrait (1080×1920) format for short-form social video, explicitly requesting "format portrait" rather than the default landscape. Confidence: 0.85
- Requests videos under 60 seconds by default, indicating a preference for short-form, high-impact content that fits social media consumption patterns. Confidence: 0.75
- Open to sub-agent parallelization — explicitly suggests "you can also create sub agents to make work fast and efficient and accurate," indicating a preference for splitting independent tasks across parallel execution paths. Confidence: 0.8
- Wants project learnings captured iteratively into the workflow guide after each completed project — explicitly instructs "capture what you learn new so you dont make mistake again," reflecting a preference for continuous improvement through documented lessons learned. Confidence: 0.8
- When updating shared documentation across multiple copies, prefers adapting inserted content to each file's existing formatting conventions (markdown table rows in tabular sections, plain bullets in list-style files) rather than force-pasting identical blocks everywhere. Confidence: 0.7
- Prefers verifying actual file state (read/grep) before retrying edits that previously failed or landed incorrectly, then applying precise targeted fixes instead of blindly re-running the failed commands. Confidence: 0.7
- Treats reference/sample images as **style guides only** — when restyling an existing video from a sample on a different topic, keeps all content, beat timings, duration, and voiceover untouched and changes visuals exclusively; never imports the sample's text/content into the target. Confidence: 0.75
- Does NOT want videos rendered automatically after changes — rendering is explicitly excluded unless requested; validation happens through the check command instead, with results reported back and any follow-up (e.g., version bumps) left for the user to approve. Confidence: 0.65
- Expects compositions to be validated with `npm run check` (HyperFrames) after every edit, fixing ALL errors and layout-overflow warnings before declaring the task complete. Confidence: 0.7
- Compound Lane Visual System (per GUIDE.md section 11) assigns fonts by role: **Fraunces** (serif) for headlines/titles/section heads, **IBM Plex Mono** (mono, with `tabular-nums`) for numbers/stats/inputs, **Inter** (sans) for body text/subtitles/labels/descriptions — applied via CSS variables (`--font-headline`, `--font-mono`, `--font-body`) with sensible font-stack fallbacks. These fonts are imported from Google Fonts and applied to specific selectors/classes rather than globally. Confidence: 0.85
- HyperFrames voiceover generation uses local Kokoro TTS via the version-pinned CLI (`npx --yes hyperframes@0.8.10 tts "<script>" --voice am_michael --speed 1.25 -o audio/voiceover.wav`); on Windows this requires setting `$env:HYPERFRAMES_PYTHON` to the Python 3.11 executable first. Confidence: 0.7
- After generating TTS, re-times the composition to the measured audio rather than the planned duration: verifies actual VO length (e.g., ffprobe), sets the `<audio data-duration>` to match exactly, shifts frame boundaries/GSAP timestamps so each visual lands while its narration line is spoken, and trims total composition length if speech comes in shorter than planned. Confidence: 0.75
- New HyperFrames video projects are scaffolded as complete self-contained folders mirroring the proven sibling project's exact file set — `package.json` (pinned-version npm scripts), `hyperframes.json`, `meta.json`, `BRIEF.md`/`STORYBOARD.md`/`SCRIPT.md`/`frame.md` planning docs, `assets/` logo copy, `capture/extracted/` source extractions, and a monolithic GSAP-timeline `index.html`. Confidence: 0.7
- Expects background music (BGM) to be part of a finished reel — after a complete voiceover-only video was delivered without music, immediately requested "Add a suitable BGM"; delegates track/mood selection to the assistant ("suitable") rather than supplying specific audio, so treat BGM selection as a standard deliverable component of future video projects. Confidence: 0.7
- Before generating BGM for a reel, expects the assistant to consult the project's audio skills/reference docs (hyperframes-audio, media-use `bgm.md`) and propose a topic-appropriate mood prompt rather than guessing — explicitly asked "check the BGM music skill, and what prompt should be added for this reel". Confidence: 0.7
- BGM is produced locally (no HeyGen credential) via the `mc` CLI musicgen-small model using a mood prompt; for finance/wealth reels the canonical calm-cinematic prompt is "calm cinematic, soft strings, subtle piano, restrained percussion" (~92 BPM), which underscores without fighting the voiceover. Confidence: 0.7
- BGM bed wiring: a non-clip `<audio id="bgm" src="audio/bgm.wav">` element on its own track at low volume (`data-volume` ≈ 0.12), generated slightly longer than the composition (e.g. a 40s bed for a 37s reel) so there is no silent tail, and ducked under the voiceover via the hyperframes-audio carve script (the classifier keys on "bgm"/"music" in the id/filename). Confidence: 0.7
- On Node 22, the hyperframes-audio carve script's `require.resolve` cannot resolve `@hyperframes/core` ESM subpath exports (`ERR_PACKAGE_PATH_NOT_EXPORTED`); the working fix is a project-local patched copy of the script that loads `audio-carve`/`audio-fx` via direct `import()`, keeping the shared skill file untouched and the project self-contained. Confidence: 0.6
- BGM generation preference: generate a short ~10-second music bed and **loop it** to cover the full video duration, rather than generating a track that matches the full video length (supersedes any prior full-length bed practice). Confidence: 0.85
- Audio effects/SFX are generated as custom cues with the `audioldm-s-full-v2` model via the `mc` CLI (`mc ai play -m audioldm-s-full-v2 -p "<cue>" -d <secs>`), overriding the skill's default of pulling from the bundled SFX library — user explicitly requested AI generation for reveal/transition cues. Confidence: 0.8
- When the user names a specific model/tool to use, expects the assistant to first verify availability and usage: check where it is referenced/required in the skill docs (e.g., sfx.md, manifest) and confirm the model is downloaded (`mc ai model list`) before generating. Confidence: 0.65
- TTS workflow must be reproducible by construction: **always** save the voiceover script text to `audio/tts_script.txt` in the project **before** running `npx hyperframes tts`, pass the file content to the tts command (rather than inline text), and keep `SCRIPT.md` in sync — explicitly instructed "Always save the TTS prompt text to audio/tts_script.txt in the project before generating the voiceover... so the voiceover is reproducible." The file's primary purpose is **user self-service editing**: the user wants to fix/correct wording by editing `tts_script.txt` (never the WAV) and regenerating, so the docs (GUIDE.md + AGENTS.md) must document that edit → regenerate (`npx hyperframes tts "$(cat audio/tts_script.txt)" ...`) → sync `data-duration` on `<audio id="vo">` → re-run `npx hyperframes check` workflow. Confidence: 0.95
- Wants a `.gitignore` at the repo root so generated/transient artifacts stay out of version control — explicitly requested "add git ignore at the root" and "add root videos folder to gitignore"; the accepted coverage includes Node artifacts (`node_modules/`, debug logs), HyperFrames build/render caches (`renders/`, `.hyperframes/`, `.thumbnails/`, `.waveform-cache/`, `capture/`, `videos/`), OS junk (`.DS_Store`, `Thumbs.db`, `desktop.ini`), and editor/IDE files (`.vscode/`, `.idea/`, swap files), verified via `git check-ignore`. Confidence: 0.75
- The expected visual format for Compound Lane educational videos is the **notebook/graph-paper hand-drawn style** — explicitly pointed at `input/sample.html` ("I like my video to be in such format… check the fonts, how the images are created learn everything, so that next time when you create the project you know what is expected"). Recipe: handwriting fonts by role — **Permanent Marker** for display titles, **Caveat** (700) for large handwritten numbers, **Patrick Hand** for body/labels (or **Kalam** as a global fallback); cream paper background (`#f7f6f3`/`#fdfbf7`) with a blue-gray 44px graph grid via two `repeating-linear-gradient`s and a red margin line (`border-left: 3px solid #ef4444`); **no `<img>` files** — all graphics are inline SVG paths + CSS (wobbly hand-drawn arrows/circles/underlines with `stroke-linecap="round"` and quadratic-bezier wobble, sketchy irregular borders via the `border-radius: 2px 255px 3px 25px / 255px 5px 225px 3px` trick, highlighter-yellow marker swipes for key "wow" numbers, hand-drawn circle around section numbers, slight rotations −2° to 3° for a hand-placed feel); color semantics: green = positive/keep, red = loss/fee/negative, blue = rules/links; motion language = write-in (fade + y + slight rotation) and SVG paths drawing themselves via `strokeDasharray`/`strokeDashoffset` with `pathLength="1"`. Canonical reference implementation: `videos/401k-notepad/index.html`. Confidence: 0.9
- Video compositions must fit a **single fixed 1080×1920 canvas with no scrolling or page movement** — when content overflows, compress/condense it to fit rather than scrolling or panning the page; the user rejected a "scrolling notebook" approach for the reel ("no scrolling. Everything stays on one fixed page"). For standalone HTML style-sample pages destined for video, the user chose **full-bleed 1080×1920** (the paper IS the frame — `body`/`.notebook-page` locked to width:1080px/height:1920px, overflow:hidden, viewport meta width=1080) over an A4 sheet floating on a desk stage when asked how the page should map onto the video frame. Confidence: 0.85
- Motion language for multi-section explainer reels (evolved): draw the entire page in fast (~2–2.5s) using pure opacity reveals + SVG path-draws so content never slides, then emphasize sections in sync with the VO. Originally the emphasis was smooth fit-to-frame camera zooms, but the user rejected the parallax zoom ("not looking good") once the layout became a space-filling infographic — zooming crops neighbors and fit-scales get small — so the entire camera system (`#scene`/`#camera`, `zoomTo()`) is now removed and content NEVER moves/scales/is cropped. Replacement emphasis (user-chosen from presented options): **spotlight dim + hand-drawn pen circle** — dim every non-active section to ~0.32 opacity while the active one stays at 1.0 (~0.5s power2.inOut), sketch a red pen-circle SVG path around the active section via `pathLength="1"` + `strokeDashoffset` draw, one beat per VO section, then clear the dim before the final full-page CTA close. Logo stays visible throughout (no zoom overlap). Marker-swipe-only, sequential-reveal, and subtle scale-pulse alternatives were declined. Confidence: 0.85
- When a camera zooms over a notebook page, the **background (graph paper, red margin, page edge) must live INSIDE the camera layer** so the whole page zooms together as one unit — a static background with only content moving reads as "floating cards over a still page" and was explicitly flagged as broken by the user. Confidence: 0.85
- The notebook format should be structured as a **properly designed infographic** — hero header (title + subtitle + stat chips with arrows), numbered content panels (soft cards with sketchy borders and circled section numbers), and a footer CTA — rather than a free-form pile of notes. Confidence: 0.75
- Notebook-style reels should **not include a progress bar** — user chose to remove it for a "pure static-notebook look". Confidence: 0.65
- Infographic page layout should **fill the canvas with no wasted space**: no red margin line (graph paper runs edge-to-edge), symmetric gutters, and smaller panels paired into 2-up flex rows to use the full width — the user approved this space-filling design over a full-width stack. When a later change (e.g. a more compact headline font) frees vertical space on the fixed 1080×1920 canvas, redistribute the freed space into breathing room — larger header/section/h2/footer margins — so the layout stays bottom-balanced (footer near the bottom edge, no dead gap), verified by re-screenshot. Confidence: 0.75
- Text must pass WCAG AA **in every visual state**, including when sections are dimmed for spotlight emphasis — labels rendered in light text inside colored bars failed contrast once dimming was introduced; fix by moving labels out as dark ink text beside the element rather than accepting the failure or suppressing it. Iterate on `check` contrast output until 0 errors / 0 warnings / full AA pass. Confidence: 0.7
- Runs CLI tooling on **Windows via PowerShell**, retrying failed shell commands with PowerShell and dropping Unix-only pipe tools (e.g. `head`) when the plain shell returns an error (Windows shell). Confidence: 0.65
- When the user names a specific AI model, verify its **actual capability** for the requested task (e.g., whether `audioldm-s-full-v2` produces speech or only SFX) by checking the model list/categories via the `mc` CLI, and recommend the correct model for the job (e.g. `bark-small` for speech synthesis) rather than assuming the named model fits. Confidence: 0.7
- When a CLI wrapper (e.g. `mc ai play`) fails for a model, do **not** take the failure as proof the model is incapable — read the wrapper's package source and invoke the model directly (Python + CUDA/float16) to reproduce/surface the real error and confirm actual capability. For Bark (`bark-small`), the `mc` wrapper's float32-CPU path is implausibly slow/broken while the model itself generates speech cleanly on CUDA/float16; prefer bypassing the wrapper with a direct CUDA script for actual TTS use. Confidence: 0.8
- After verifying a workflow works end-to-end, expects the assistant to provide a **reusable standalone sample script saved as a file** (e.g., a `bark_tts.py` in the scratchpad) plus the exact command to run it, so the user can self-test locally on their own ("give me the sample ill run it to test") rather than relying solely on the assistant's verification. Confidence: 0.7
- Wants the runnable command given as a **bare, copy-pasteable command with minimal/no surrounding prose** — after the sample was written, pushed back twice with "give me the sample I'll run it to test" and "give me the cmd directly", signaling a preference for terse, direct, ready-to-run commands (in the active shell) over explanatory paragraphs. Confidence: 0.7
- Prefers to invoke models through the **simple single `mc ai play` CLI one-liner** rather than running/editing Python scripts directly — explicitly asked "cant we run without the python - eg - mc ai play -m bark-small -p ... ?". Prefer working to make the simple CLI command work (even if it requires patching the wrapper, e.g. disabling the CPU-offload/device-mismatch bug and stripping the loop-appended prompt) over handing the user direct Python scripts, since the user wants minimal setup and no direct Python invocation. Confidence: 0.8
- Wants generated scripts/tools to run **warning-free** — when running `bark_tts.py` surfaced transformers attention-mask and pad-token warnings, explicitly requested "let resolve the warnings/attention notes", expecting the underlying cause (e.g., unset `pad_token_id` falling back to `eos`) to be diagnosed and fixed so output is clean. Treat suppressing/eliminating runtime warnings as a required part of a working script, not optional noise. Confidence: 0.7
- Holds AI-generated media to an **audible-quality bar** — after a functionally-correct Bark TTS run, immediately challenged the result ("why thre is background noise in the output?"), treating audible artifacts as defects to be root-caused and fixed, not accepted quirks. Expects diagnosis of the cause plus concrete remediation options (e.g., voice presets for cleaner/consistent speakers, post-processing such as high-pass + noise-gate denoising) offered before proceeding. Confidence: 0.7
- Accepted clean-bark-small VO recipe, baked into the reusable `bark_tts.py`: speaker preset `--voice v2/en_speaker_6` (consistent narrator, much cleaner than unprompted Bark) combined with a `--denoise` pass (4th-order butter high-pass at 80 Hz + downward-expander noise gate at ≈−45 dBFS relative to peak); user explicitly approved extending the shared script with both flags ("yes, update the script") rather than doing ad-hoc cleanup. Implementation gotcha: with a voice preset, the processor's `history_prompt` nested tensors stay on CPU after `.to("cuda")` and must be moved to device explicitly. Confidence: 0.7
- When a visual style reference is supplied as a **saved web page**, mine its complete saved-asset bundle (the `*_files/` directory): the extracted external stylesheet for CSS variables/utility recipes, the `css2` response for the real Google-Fonts families in use, inline `<svg><defs>` filters, and grep the minified body for utility-class usage — then present a technique-comparison table (reference vs current page) showing exactly what can be picked up before proposing changes. Confidence: 0.7
- The "authentic handwritten notebook" bar is met through a signature texture kit mined from the reference page, all user-approved: rough-ink SVG filters (`feTurbulence type="fractalNoise"` + `feDisplacementMap` with fixed seeds ⇒ deterministic/video-safe; scale 2.6 strong `#rough-ink` / 1.4 soft `#rough-ink-soft`) wobbling box borders, arrows, section-number blobs and squiggles via `.rough`/`.rough-soft` classes; paper-grain data-URI noise-tile overlay (opacity .4, `mix-blend-mode: multiply`); ink-bleed `text-shadow: 0 0 .6px currentColor` on written text; organic geometry (blob radii `48% 44% 50% 46%`, irregular card radii like `2.4rem 2rem 2.6rem 2.2rem`, dashed hand borders). Confidence: 0.8
- Color direction for the handwritten-notebook style: semantic oklch ballpoint ink palette (`--ink-navy/-maroon/-green/-blue` + `-2` variants) with remaps (red→maroon for fees/losses, green→positives, blue→rules/links) and low-opacity washes via `color-mix(in oklab, var(--ink-X) 4.5%, transparent)`. After the user rejected the first pass as "giving a very old look", the settled direction is **fresh & modern, never vintage/sepia**: crisp near-white paper `oklch(98.7% .004 250)`, a LIGHT cool-gray radial desk (`oklch(94% → 91%)`) with soft shadow (the dark charcoal desk + deep shadow combo was explicitly rejected), brighter saturated inks (navy `oklch(44% .17 268)`, body `oklch(46% .11 264)`, maroon `oklch(52% .19 25)`, green `oklch(52% .14 158)`, blue `oklch(52% .18 258)`) instead of muted vintage tones, and paper grain dialed down to opacity .25. The hand-drawn character (rough-ink SVG filters, blob circles, sketchy borders, highlighter swipes, slight rotations, ink bleed) survives every restyle untouched. Confidence: 0.8
- For ruled-notebook pages the user wants **horizontal ruled lines, not a graph grid** — explicitly requested "lets keep Horizontal rules lines". Recipe: `repeating-linear-gradient(to bottom, transparent 0, transparent calc(var(--line-height-rule) - 1.2px), var(--rule) var(--line-height-rule))` with `--line-height-rule: 1.95rem` and a light-blue rule `oklch(88% .04 255)`; bind text to the lines via `line-height: var(--line-height-rule)` on body copy, list items (margin-bottom zeroed), table rows (padding/borders stripped), labels, result rows, and footer so handwriting sits exactly on the rules. Supersedes the earlier 30px/44px graph-grid treatment for these pages. On the video-canvas version the red vertical margin rule was removed on explicit request ("remove the red margine") — keep only the subtle shaded left band; expect NO red margin line on these notebook pages. Confidence: 0.85
- For the standalone style-sample page (`input/sample.html`), the user chose the **reference font trio** over the video-system fonts: Architects Daughter (headings), Patrick Hand (body/labels/inputs/table), Caveat 600/700 (script accents + big handwritten numbers), with Kalam demoted to fallback-only — wired as per-role CSS variables (`--font-head/--font-hand/--font-script`) instead of a global font-family rule. Confidence: 0.7
- Styling-task constraints: content stays byte-identical (copy, section order, IDs, calculator JS logic) — only fonts/colors/textures change; output must be a **single self-contained HTML file** (no Tailwind, no external CSS/JS/artifact badges; the only permitted network fetch is the Google Fonts `<link>`); rendering must remain **deterministic** for video capture (fixed SVG filter seeds, no `Math.random()`/`Date.now()`). Confidence: 0.7
- Verify styled-HTML work visually before declaring done: headless-browser screenshot (`msedge.exe --headless --disable-gpu --window-size=W,H --screenshot=temp\after.png "file:///…"`; fall back to plain cmd syntax when the PowerShell call operator errors), read the PNG back and inspect it, and sanity-check that JS-rendered values still match expected outputs (proves script untouched). Confidence: 0.7
- New Compound Lane page content arrives as a **structured JSON spec** (`handle`, `title`, `series_label`, numbered `concepts[]` each with `title`/`body`/`visual_type` + `visual_spec`, `sources_footer`) — delivered as "update the sample.html with this new content - {...}" — and the expectation is to rebuild `input/sample.html` rendering every concept on the fixed 1080×1920 notebook canvas. Map each `visual_type` onto the hand-drawn component library: table → sketchy-bordered `<table>` with solid ink-navy/blue header row and highlighter+maroon flagging of "bad" rows; comparison → green-vs-maroon side-by-side panels with label chips and a Caveat "vs" badge; callout → yellow-highlighter ⚠ WARNING band; diagram → flow nodes + rough SVG squiggle arrows; none → plain body text. Confidence: 0.75
- When swapping in new topic content, keep the notebook design system stable (CSS variables, ink palette, ruled lines, rough-ink filters, paper grain, blob section numbers, font trio, highlighter swipes) and change only content/layout/components; for denser pages scale type down (body ~1.2rem, h2 ~1.6rem, 40px blobs), give panels semi-opaque paper backgrounds sitting over the ruled texture while plain text stays bound to the rules, and drop the `<script>` block entirely when the new page needs no interactivity. Confidence: 0.75
- Prefers per-article project scaffold under `projects/{slug}/` with numbered stages `01-source/` (source.html + source_files/), `01-content/{slug}.json`, `02-visual/{slug}.html/.png` (fork of global template), and optional `03-video/` over flat `input/` dumping; uses lowercase `projects` and kebab-case slugs with `01-`/`02-` prefixes for deterministic sort order. Confidence: 0.85
- Prefers global visual-notes template at `templates/notebook-v2.html` (mirrored at `input/sample_v2.html`) with `input/sample.html` as throwaway preview mirror of the latest `02-visual` render; explicitly flagged when `templates/` folder was missing ("template folder not created"), expecting a dedicated `templates/` directory for the canonical template. Confidence: 0.85
- Prefers automatic migration of existing assets into a new project structure when the workflow evolves (e.g., auto-moving `input/4`, `input/*.json`, and current render into `projects/{slug}/` as first example) and signals approval concisely with "Approved / go ahead" to proceed without further confirmation. Confidence: 0.75
